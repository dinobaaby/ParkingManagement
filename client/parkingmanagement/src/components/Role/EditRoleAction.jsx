import { Button } from "@mui/material";
import React from "react";

export default function EditRoleAction() {
    return (
        <Button size="small" variant="contained" color="warning">
            Edit
        </Button>
    );
}
